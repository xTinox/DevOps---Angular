import { ChangeDetectorRef, ElementRef, EventEmitter, ViewContainerRef } from '@angular/core';
import { Binding, Bindings, PartialPointsBinder, UndoHistoryBase } from 'interacto';
import { OnDynamicDirective } from './on-dynamic.directive';
import { InteractoBinderDirective } from './interacto-binder-directive';
import * as i0 from "@angular/core";
export declare class ClicksBinderDirective extends InteractoBinderDirective<HTMLElement, PartialPointsBinder> {
    private bindings;
    private readonly clicksBinder;
    constructor(onDyn: OnDynamicDirective, element: ElementRef<HTMLElement>, viewContainerRef: ViewContainerRef, changeDetectorRef: ChangeDetectorRef, bindings: Bindings<UndoHistoryBase>);
    /**
     * The expected number of clicks.
     */
    count: number | string;
    /**
     * Starts the creation of a binding using the clicks interaction.
     * @param fn - The function of the component that will be called to configure the binding.
     */
    set ioClicks(fn: ((partialBinder: PartialPointsBinder, widget: HTMLElement) => Binding<any, any, any> | Array<Binding<any, any, any>> | void) | undefined | string);
    protected createPartialBinder(): PartialPointsBinder;
    protected getOutputEvent(): EventEmitter<PartialPointsBinder>;
    static ɵfac: i0.ɵɵFactoryDeclaration<ClicksBinderDirective, [{ optional: true; host: true; }, null, null, null, null]>;
    static ɵdir: i0.ɵɵDirectiveDeclaration<ClicksBinderDirective, "[ioClicks]", never, { "count": "count"; "ioClicks": "ioClicks"; }, { "clicksBinder": "clicksBinder"; }, never, never, false>;
}
