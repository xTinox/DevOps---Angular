import { MatSelect, MatSelectChange } from '@angular/material/select';
import { MatButtonToggle, MatButtonToggleChange } from '@angular/material/button-toggle';
import { Flushable } from 'interacto';
export declare type SupportedMaterial = MatSelect | MatButtonToggle;
export declare type SupportedMaterialChange = MatSelectChange | MatButtonToggleChange;
export declare class MatEvent<T extends SupportedMaterialChange> extends Event {
    readonly change: T;
    constructor(change: T);
}
export interface MatChange<T extends SupportedMaterialChange> {
    change: T | undefined;
}
export declare class MatChangeImpl<T extends SupportedMaterialChange> implements MatChange<T>, Flushable {
    _change: T | undefined;
    constructor();
    get change(): T | undefined;
    flush(): void;
}
