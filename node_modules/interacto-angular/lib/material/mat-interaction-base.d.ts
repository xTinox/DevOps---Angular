import { FSM, InteractionBase, Logger } from 'interacto';
import { Subscription } from 'rxjs';
import { MatChange, MatChangeImpl, SupportedMaterial, SupportedMaterialChange } from './mat-event';
export declare abstract class MatInteractionBase<T extends SupportedMaterialChange> extends InteractionBase<MatChange<T>, MatChangeImpl<T>, FSM> {
    currentSubscription: Subscription | undefined;
    protected constructor(logger: Logger, fsm: FSM, data: MatChangeImpl<T>);
    protected registerEventToMatObject(node: SupportedMaterial): void;
    protected unregisterEventToMatObject(): void;
    onNewNodeRegistered(node: unknown): void;
    onNodeUnregistered(node: unknown): void;
    reinit(): void;
}
